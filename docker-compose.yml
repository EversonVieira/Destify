services:
  postgres:
    image: postgres:14
    container_name: postgres-db
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: localpassword
      POSTGRES_DB: destify
    ports:
      - "5432:5432"
    volumes:
      - destify:/var/lib/postgresql/data

volumes:
  destify:

